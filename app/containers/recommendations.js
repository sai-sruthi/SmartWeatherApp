import React, { useState, useEffect, Component } from 'react';
import { View, Text, TouchableWithoutFeedback, Keyboard, AsyncStorage, ScrollView } from 'react-native';
import styles from '../styles';
import Login from '../components/login';
import Register from '../components/register';
import { authenticateUser as authUser, registerUser as regUser, saveUser } from '../services/welcomeService';
import { Notifications } from 'expo';
import { render } from 'react-dom';
import ReactDOM from 'react-dom';

export default function Recommendations({navigation}){
      return (
        <View>
        <Text>
          a
          a
          authenticateUsera
          a
          Hello!kjdnslvjndvindpuf oilsudfj goerngiufdniug udgiredbgfijdkfnig eriugniurngin dfin irhegneoirdgni udfngfidn
        </Text>
        </View>
      );
    
  }